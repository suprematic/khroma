(ns khroma-tests.identity
  (:require
    [devcards.core :as core])
  (:require-macros
    [devcards.core :as dc :refer [defcard defcard-doc mkdn-pprint-source]]))


(defcard
  "#khroma.identity"
  "All `khroma.identity` functions are authentication-flow oriented, so I
  can't really demonstrate them in the abstract. If anyone has a good use
  example, pull requests are welcome.

  Meanwhile, check Chrome's [identity documentation](https://developer.chrome.com/apps/identity).")


